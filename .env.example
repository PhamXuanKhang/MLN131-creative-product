# Bí mật phía server cho /api/chat — KHÔNG commit giá trị thật.
# Local: copy thành .env rồi điền các giá trị, chạy bằng `vercel dev`.
# Production/Preview: thêm OpenAI key trong Vercel Environment Variables.
OPENAI_API_KEY=
